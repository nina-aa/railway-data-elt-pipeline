# Exact versions used to build this project (Python 3.12).
#   python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
duckdb==1.5.5
dbt-core==1.12.4
dbt-duckdb==1.11.0
requests==2.34.2
matplotlib==3.11.1
